# docx 链干净样例: 行式题注/链题注/公式编号三线全闭环, 白名单与围栏豁免齐备。

# 摘要
模型给出 18 个节点的温度场, 误差为 0.16。

# 符号说明
| 符号 | 含义 | 单位 |
|------|------|------|
| $C$ | 含水率 | kg/kg |
| $r_i$ | 网格节点 | m |
| $\alpha$ | 扩散系数 | m2/s |

# 模型建立与求解
式（1）与式(2)联立求解, 初值条件为式（3）; 收敛过程如图 1 与图 2 所示,
参数与结果见表 1 与表 2, 网格与求解设置见图 3 并汇总于表 3。
全部结果经独立脚本逐格核对 (结果表 50 格逐格), 无不一致。

$$C=\alpha\,r_i \qquad (1)$$

$$r_i=\frac{\alpha C}{r_i} \qquad (2)$$

$$
r_i(r,0)=\alpha C,
\qquad (3)
$$

![图 1 温度场演化](figures/q1.png){width=95%}

![图 2 收敛阶](figures/q2.png){width=75%}

: 表 1：物性参数

| 参数 | 取值 |
|------|------|
| 密度 | 820 |

: 表 2：数值结果

| 节点数 | 误差 |
|------|------|
| 41 | 0.16 |

图 3：网格示意

表 3：求解参数

哑变量 $i$、$j$、$e$、$a$、$b$、$x$、$t$ 与行内代码 `delta_w` 均不触发符号告警。

~~~
围栏内的 图 99、表 99、式（99） 与 $w$ 都不算引用:
```python
y = alpha  # 围栏内
```
~~~

````python
四反引号围栏内的 ``` 行不是关栏, 其中的 图 98 同样不计引用。
````

# 结论
模型给出 18 个节点的温度场, 误差为 0.16。
